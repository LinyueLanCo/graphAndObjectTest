# 对话迁移总结

更新时间：2026-06-09

这份文档用于把当前 EasyX/C++ 2D 游戏项目的讨论上下文迁移到新对话中。早期对话经过上下文压缩后保留下来的是摘要，不是逐字记录；最近关于 Tiled、组件化、视差背景的讨论是当前线程中较完整的上下文。

## 项目位置与当前状态

项目主目录：

```text
C:\Users\lpy16\OneDrive\Desktop\c++\GraphAndObjTest\graphAndObjectTest
```

当前 Git 状态大致为：

```text
master...origin/master [ahead 7]
 M assets/tex/maps/map.txt
 M assets/tex/maps/tileset.png
 M game.cpp
?? assets/tex/maps/Clouds 2/
```

注意：

- `game.cpp` 有用户近期关于输入切换控制对象、背景视差等未提交改动。
- `assets/tex/maps/tileset.png` 是用户扩展后的新 tileset。
- `assets/tex/maps/Clouds 2/` 是新加入的四层视差背景素材。
- `assets/tex/maps/map.txt` 已多次从 Tiled 地图转换/重导出。
- 不要随意覆盖这些未提交内容。

## 已完成的重要重构

### Entity 精简

最早的 `Entity` 承担了大量职责，包括：

- 自己绘制图片。
- 自己处理动画切换。
- 自己计算碰撞盒。
- 自己加载部分资源路径。
- 自己保存大量状态布尔值。

经过多轮讨论和修改后，方向已经变成：

- `Entity` 仍然保存实体自己的基础数据，比如位置、速度、状态、碰撞盒、动画组件对象等。
- 绘制行为迁移给 `Renderer`。
- 碰撞盒数据和计算逻辑逐步迁移给 `CollisionBox` / `RectBox` 相关结构。
- 动画状态切换逐步迁移给 `Animator`。
- 图片和动画资源逐步由 `ResourceManager` 管理。
- `Entity` 不应该再直接写死图片路径。

当前 `Entity` 已经比最初精简很多，但仍然不是完整组件化结构。它现在更像“持有若干功能对象的实体类”，而不是 ECS 那种“只靠 id 串联组件数据”的结构。

### Renderer 统一绘制

已经明确的设计判断：

```text
Renderable object 自己不负责 draw。
Renderer 统一负责 draw 各类可渲染对象。
```

当前数据流大致是：

```text
Entity / TileMap / UI 提供可绘制数据
-> Renderer 根据 Camera 转换坐标
-> Renderer 调用 EasyX / AlphaBlend / putimage 等底层绘制函数
```

这个方向避免了绘制逻辑散落在各种对象里，也更适合后续替换 EasyX 渲染层。

### CollisionBox / Tile Collision

已经实现了 tile 碰撞第一版：

- `TileMap` 根据 tileId 生成默认碰撞类型。
- 支持 full solid。
- 支持 one-way platform。
- 支持 top-half / top-left-half / top-right-half 之类的裁剪碰撞盒。
- 碰撞 debug 框能显示真实裁剪后的 tile collision box。
- `CollisionHandle` 在 X/Y 方向碰撞计算时区分 solid 与 one-way。

关于 tile 碰撞的共识：

- tile 绘制层和 tile collision layer 可以分开。
- tile 不是每个都必须是独立对象，默认可以只是二维数组中的数据。
- 真正需要行为/状态/触发的东西更适合做成 GameObject / Entity / Trigger，而不是普通 tile。
- tile 碰撞可以先用少量预设规则，而不是为每个 tile 画非常精确的形状。

### Animator / AnimationClip / AnimatedSprite

围绕动画系统形成的职能边界：

```text
AnimationClip
- 定义动画资源和帧数据。
- 记录图片引用、总帧数、单帧宽高、间隔、播放顺序等配置。
- 不负责每帧业务逻辑。

Animator
- 根据 Entity 的逻辑状态选择当前动画状态。
- 类似 Unity Animator / UE 动画蓝图中的状态机概念。
- 读取 Entity 的速度、是否在空中、朝向等数据。
- 不应该计算会反过来影响玩法逻辑的状态。

AnimatedSprite / AnimationPlayer
- 更应该变成纯播放器。
- 根据当前 AnimationClip 和播放时间计算当前帧的裁剪坐标。
- 不应该管理资源加载，也不应该决定业务状态。

Sprite
- 记录当前绘制帧需要的图片引用、源图裁剪区域、世界绘制区域、缩放/偏移等。

ResourceManager
- 统一加载图片、AnimationClip、未来的配置文件。
```

关于动画资源复用的判断：

- 像素 2D 中，不同角色通常不能直接共用同一套图片动画。
- 可以共用 `AnimationState` 概念，例如 `IDLE / WALK / RUN / JUMP_LOOP`。
- 但每个角色应该拥有自己的 animation set / animation clip map。
- 数字键切换控制角色时，这一点已经体现出来：控制权切换不能导致角色视觉身份变成 player1。

### 跳跃和移动状态机

已经完成过一套基础移动/跳跃动画状态逻辑：

- idle
- walk
- run
- jump start
- in air / jump loop
- jump end

关键判断：

- `jump start` 只应由主动输入 intent 引导。
- 如果被弹簧/蘑菇等地图元素弹起，不应该触发 jump start，而应直接进入 in air / jump loop。
- 如果头顶碰撞打断起跳，这属于 Entity / Movement / Collision 的逻辑，不应该由动画状态来判断。
- 落地时如果 X 方向仍有速度，可以直接接 walk/run，而不是强制播放 jump end。

## 当前 Level / System / Component 理解

### Level 的定位

`Level` 是当前关卡/场景拥有者：

- 拥有当前 tileMap。
- 拥有当前 entity 容器。
- 拥有当前 camera / renderer / UI。
- 负责初始化当前关卡。
- 每 tick 调度 update。
- 每帧调度 draw。

### System 层的定位

后续如果引入 system 层，它应该位于 `Level` 之下：

```text
Game
-> 当前 Level
   -> InputSystem
   -> MovementSystem
   -> CollisionSystem
   -> AnimationSystem
   -> EventSystem
   -> RenderSystem
```

判断标准：

```text
处理单个对象内部能力 -> Component / Handle
处理一批对象的同类规则 -> System
拥有并调度这些规则 -> Level
```

### 组件化路线

当前结构还没有真正组件化。现在更像：

```text
Entity
- Animator animator
- MovementHandle movementHandle
- CollisionBox collisionBox
- 一堆状态数据
```

这已经不是“Entity 全包”，但组件仍然作为成员对象固定长在 Entity 身上。更完整的组件化可以分阶段：

第一阶段：

```text
Entity 按需持有不同组件对象。
例如有些 Entity 有 Animator，有些只有 LoopAnimator，有些没有 Animator。
```

第二阶段：

```text
Entity 只保留 id / transform / 基础身份。
AnimatorComponents / MovementComponents / CollisionComponents 等独立容器用 entityId 关联。
```

第三阶段：

```text
System 遍历组件容器，根据 id 读取和写入数据。
组件尽量只保存数据，真正干活的是系统。
```

这个方向接近 ECS，但不需要立刻完整 ECS 化。

## Trigger / Event / Director 构想

已经形成的方向：

```text
Trigger 负责判断某件事是否发生。
Event 负责描述发生了什么。
Handler / Listener 负责执行发生之后要做什么。
```

示例：

```text
玩家进入区域
-> TriggerBox 产生 OnTriggerEnter
-> EventSystem 分发事件
-> Handler 打开门 / 播放音效 / 移动镜头 / 生成敌人 / 推进剧情变量
```

Trigger/Event 不应该替代所有 update。它更适合离散事件：

- 进入区域。
- 拾取物品。
- 生命归零。
- 落地。
- 按钮被按下。
- 演出结束。
- 镜头到达目标点。

连续系统仍然应该每帧更新：

- movement
- collision
- animation frame
- camera follow
- render

后续 `Director` 类可以用于关卡演出、镜头调度、脚本动作序列，与 EventSystem 配合。

## 关卡编辑器构想

用户认为在 Tiled、map.txt、tileset.png 之间来回折腾太麻烦，后续希望做游戏内编辑器。

已经形成的设计：

```text
EditorCamera
- freecam entity 或独立 camera anchor。

EditorUI
- 右侧 tile 面板。
- entity 面板。
- 层级切换。
- 保存按钮。

EditorInput
- 鼠标点击、拖拽、键盘快捷键、滚轮。

EditorSelection
- 当前选中的 tile / entity / brush / layer。

EditorTool
- 画 tile、擦除、选择、移动实体、放置实体。

MapData
- tileLayers
- collisionLayer
- entitySpawns
- triggerObjects
- eventBindings
- cameraSettings
- parallaxLayers

EditorSaveLoad
- 保存 map.txt / json / 自定义关卡文件。
```

关键判断：

- tile 放置严格按二维数组 index 执行。
- 鼠标处的选中 tile 预览只是视觉效果，不应直接修改地图数据。
- entity 放置应该支持网格吸附和精确坐标两种模式。
- tile 编辑和 entity 编辑不应共用同一套数据结构。
- 如果编辑器只服务于开发者本人，它可以扩展成内部 game editor，而不只是简单 level editor。
- 编辑器负责编辑数据和参数，不负责临时创造底层 C++ 逻辑。

## Tiled / Tileset / Map 修复记录

用户多次扩展 `tileset.png`，导致 Tiled 中两套 tileset 混用：

```xml
<tileset firstgid="1" source="Terrain (16x16).tsx"/>
<tileset firstgid="243" source="tileset.tsx"/>
```

项目游戏侧只想使用单张大的：

```text
assets/tex/maps/tileset.png
```

已经处理过：

- 将 Tiled 的全局 gid 转换为单张 tileset 的本地 id。
- 对于 `gid >= 243`，转换为 `gid - 242`。
- 将 `test2.tmx` 改为只引用：

```xml
<tileset firstgid="1" source="tileset.tsx"/>
```

- 更新 `tileset.tsx` 为：

```xml
tilecount="352" columns="22"
width="352" height="256"
```

- 修复过 TMX CSV 格式问题：Tiled 的 CSV 是连续一维列表，前 49 行末尾需要逗号，否则跨行解析会粘连，导致图层数据损坏。
- 已生成备份：

```text
C:\Users\lpy16\OneDrive\Desktop\assets\TiledProject\test2.tmx.bak
C:\Users\lpy16\OneDrive\Desktop\assets\TiledProject\tileset.tsx.bak
```

## Freecam / 切换控制对象

用户已经实现数字键切换操控角色。

当前理解：

```text
InputManager
-> 只把输入意图交给当前 controlTarget entity
-> Entity 更新移动/状态
-> Animator 根据该 Entity 自己的数据切换动画
-> Renderer 绘制该 Entity 自己绑定的资源
```

这个功能验证了“控制权”和“实体视觉身份”应分离。

关于 freecam 的构想：

- 可以先用一个不可见、可 overlap、god 模式实体作为临时 freecam anchor。
- 更干净的长期方式是独立 `FreeCameraAnchor` 或 `CameraController`。
- 关键功能是切换输入控制对象，而不是非要让 freecam 伪装成普通角色。

## 当前正在处理：视差背景

用户把四张背景素材放入：

```text
assets\tex\maps\Clouds 2
```

四张图：

```text
1.png
2.png
3.png
4.png
```

判断：

- 这是明显的多层视差背景素材。
- `1.png` 是最远天空层。
- `2.png` 是远处云/雾层。
- `3.png` 是远山群。
- `4.png` 是更近的大云/山层。

当前已做：

- `Level` 中用 `IMAGE backgrounds[4]` 替代单张 `background`。
- `initBackground()` 加载四张图。
- `Renderer::drawBackground()` 按顺序绘制四层。
- 已加入实时视差偏移和 X 方向循环平铺。
- 视差效果能正常显示。

遇到的问题：

1. 性能明显变慢。

原因：

```text
EasyX / GDI 每帧多次全屏 AlphaBlend + 缩放绘制很吃 CPU。
```

优化方向：

- 加载时预缩放到窗口大小。
- 第一层不透明背景用普通 `putimage`。
- 静态背景可以预合成一张缓存图。
- 真正视差层保留实时多层绘制。
- 后续如果追求大量透明、粒子、旋转、缩放，EasyX 性能上限会成为瓶颈。

用户当前更重视效果，所以暂时不优先做缓存优化。

2. 背景跟随 zoom、鼠标 offset、camera offset 产生反直觉偏移。

原因：

```text
当前 drawBackground 直接读取 gCamera.x / gCamera.y。
但 gCamera.x/y 混合了：
- 玩家跟随
- 鼠标 look offset
- zoom 引起的可见范围变化
- camera 平滑跟随
```

这对世界物体是正确的，但对远景背景不一定直觉。

下一步建议：

先不要直接用 `gCamera.x` 作为视差依据，而是引入更稳定的视差锚点。

短期方案：

```cpp
double cameraCenterX = gCamera.x + gCamera.getVisibleWorldWidth() / 2.0;
```

注意当前 `Camera` 函数名是：

```cpp
getVisibleWorldWidth()
getVisibleWorldHeight()
```

不是 `getViewWidth()`。

但这仍然会受 zoom 影响。

更好的方案：

```text
让 Level 维护 parallaxCameraX。
parallaxCameraX 只跟随当前 camera target / controlled entity 的世界 x。
它不吃 mouse look offset，也不吃 zoom。
Renderer 绘制背景时读取 parallaxCameraX。
```

可能的数据流：

```text
Level::update()
-> updateCamera()
-> updateParallaxCamera()

Level::draw()
-> renderer.drawBackground(backgrounds, 4, parallaxCameraX)
-> renderer.drawTileMap(tileMap)
-> renderer.drawEntities(entitys)
```

## 当前最适合继续的任务

### 任务 1：修正视差背景锚点

目标：

```text
视差背景只跟随角色/关卡横向推进，不跟随 zoom 和鼠标 look offset 产生反直觉漂移。
```

建议：

- 在 `Level` 加一个 `double parallaxCameraX;`
- 初始化为 0。
- 每帧根据当前相机跟随目标的 Entity X 更新它。
- 传给 `Renderer::drawBackground()`。
- `Renderer` 不再直接读取 `gCamera.x` 计算背景 X。

### 任务 2：保留效果的前提下做轻量优化

在视差效果确认后，可以考虑：

- 四张图加载时预缩放。
- 第一层用 `putimage`。
- 只有透明层用 `putimage_alpha`。
- 不要每帧做图像缩放。

### 任务 3：保存当前阶段并提交

当前有多个未提交改动。完成视差背景第一版后可以：

- 检查 `game.cpp` 注释。
- 更新 `README.md`。
- 更新 `修改日志.md`。
- 提交本地 commit。

### 任务 4：回到组件化

视差背景完成后，主线目标仍然是：

```text
Entity -> GameObject / Component / System 的进一步拆分。
```

重点是：

- 不同实体按需挂载不同组件。
- Animator 多态化或组件化。
- MovementHandle 从 Entity/Level 中理清归属。
- 为后续 Trigger/Event/Director 做铺垫。

## 对用户设计思路的评价

用户的架构理解明显在成长，特点是：

- 非常关注数据流向。
- 能主动发现职责边界不清的问题。
- 能意识到“功能拆分”和“真正组件化”不是同一回事。
- 能从 Unity、UE、Minecraft、Evoland 2 等经验反推自己的框架需求。
- 对数据驱动、事件驱动、编辑器工具链的需求不是凭空想象，而是从当前开发痛点自然产生的。

几个已经确认的好判断：

- `Level` 不应该亲自承担所有 update 业务逻辑。
- `Renderer` 统一绘制是正确方向。
- `Entity` 持有 CollisionBox 可以，但计算逻辑不该全塞在 Entity。
- `Animator` 应读取 Entity 状态来判断动画，不应生成影响玩法逻辑的状态。
- 角色朝向不应由 Animator 负责，因为它会影响射线、攻击、交互等玩法逻辑。
- 资源路径不应散落在 Entity 初始化里。
- 数字键切换控制对象验证了实体视觉身份和输入控制权应该分离。
- Tiled 和 map.txt 来回折腾说明需要更统一的编辑/导出工具链。

## 新对话接棒提示

如果迁移到新对话，可以直接贴这段：

```text
这是我的 EasyX/C++ 2D 游戏项目。当前项目主要在 game.cpp 中，路径是：
C:\Users\lpy16\OneDrive\Desktop\c++\GraphAndObjTest\graphAndObjectTest

我已经完成了 Renderer、CollisionBox、Animator、ResourceManager、TileMap 碰撞、数字键切换控制对象等一系列重构。当前还没有完整 ECS 组件化，但正在往组件化、数据驱动、Trigger/Event、内部关卡编辑器方向设计。

当前正在处理视差背景：
我已经把四张背景图放在 assets\tex\maps\Clouds 2，Level 里用 IMAGE backgrounds[4] 加载，Renderer::drawBackground 负责绘制四层，并已经实现了实时视差和横向循环平铺。现在的问题是背景会跟着 gCamera 的 zoom、鼠标 look offset、camera offset 产生反直觉漂移。下一步想让视差背景使用独立 parallaxCameraX，只跟随角色/关卡横向推进，不直接吃 gCamera.x/y。

请先检查当前 game.cpp，再一步步指导我继续。
```
